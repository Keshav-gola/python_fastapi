name: Build and Deploy Code

on: [push, pull_request]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v4

      - name: say hi to keshav
        run: echo "hi keshav"

      - name: Install python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'